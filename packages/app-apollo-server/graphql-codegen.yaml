schema: 'src/**/*.graphql'
hooks:
  afterAllFileWrite:
    - prettier --write
generates:
  ./src/generated.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      typesPrefix: GQL
      maybeValue: 'T | undefined'
      enumsAsTypes: true
